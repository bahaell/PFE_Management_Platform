server:
  port: 8086 # Chat service port (notifications is on 8085)
  servlet:
    context-path: /api/chat

spring:
  application:
    name: chat-service
    
  datasource:
    url: jdbc:postgresql://localhost:5432/pfe_chat_db
    username: postgres
    password: 200104ba++
    driver-class-name: org.postgresql.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# Firebase Admin SDK config path
firebase:
  credentials:
    path: classpath:firebase-key.json

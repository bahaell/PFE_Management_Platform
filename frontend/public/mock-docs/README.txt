This folder contains mock-generated documents (HTML previews).

For production-ready PDF generation, integrate Puppeteer or Playwright in a server API and replace the mock generator in `services/service_templates.ts`.
